/**
 * Registers every adapter.
 *
 * Imported for side effects by the CLI scripts and the cron routes. Keeping the
 * registrations in one file means the pipeline runner never needs to know what
 * sources exist.
 */

// Adapters are registered here as each is implemented (Epic E3).
export {};
